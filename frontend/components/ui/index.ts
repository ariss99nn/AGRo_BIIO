/**
 * Barrel export de componentes UI
 * 
 * Permite importar todos los componentes desde un solo lugar:
 * import { Button, Card, Input, Tag, Loading, Empty, ErrorState } from '@/components/ui';
 */

export { default as Button } from './Button';
export { default as Card } from './Card';
export { default as Input } from './Input';
export { default as Tag } from './Tag';
export { Loading, Empty, ErrorState, Skeleton, SkeletonCard } from './States';
